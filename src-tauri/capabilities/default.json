{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The application window needs nothing from the system beyond being shown.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
